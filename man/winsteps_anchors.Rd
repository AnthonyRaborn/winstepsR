% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchors.R
\name{winsteps_anchors}
\alias{winsteps_anchors}
\title{Bundle item names with their anchor values}
\usage{
winsteps_anchors(items, values)
}
\arguments{
\item{items}{Character vector of item names. Must be unique and free of
tabs and semicolons.}

\item{values}{Numeric vector of anchor values (e.g. IRT b-parameters), the
same length and order as \code{items}. Must all be finite.}
}
\value{
An object of class \code{winsteps_anchors}: a list of \code{items} and
\code{values}, which prints as a summary of the bank.
}
\description{
\code{items} and \code{values} are two vectors that must stay in the same order:
sequence number is position, so the anchor file, the item subset file and
the data file all index items the same way. Keeping them in one object
makes that invariant structural rather than something each function
re-checks and each caller has to remember.
}
\details{
Validation happens once here rather than in every writer, so a duplicated
item name, a name carrying the anchor file's own delimiters, or a missing
anchor value is caught at the point the bank is assembled -- before any
file is written.
}
\examples{
anchors <- winsteps_anchors(c("q1", "q2", "q3"), c(-0.4, 0.1, 0.6))
anchors
anchors$items
}

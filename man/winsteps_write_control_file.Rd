% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control_file.R
\name{winsteps_write_control_file}
\alias{winsteps_write_control_file}
\title{Write a Winsteps control file}
\usage{
winsteps_write_control_file(
  file,
  data_file,
  n_items,
  item1,
  name1 = 1,
  delimiter_width = 1,
  namlen = item1 - name1 - delimiter_width,
  codes = "01",
  iafile = NULL,
  idfile = NULL,
  pfile = "person.out",
  ifile = NULL,
  item_labels = NULL,
  estimation = list(),
  tfile = NULL,
  extra = character(0)
)
}
\arguments{
\item{file}{Path to write the control file to.}

\item{data_file}{Value for \verb{DATA=} -- the person-response data file
written by \code{\link[=winsteps_write_person_data]{winsteps_write_person_data()}}, as Winsteps will see it
(typically just the file name, since the control file and data file
are usually run from the same working directory).}

\item{n_items}{Number of items (\verb{NI=}). Use \code{n_items} from
\code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}}.}

\item{item1}{Column position of the first item response (\verb{ITEM1=}).
Use \code{item1} from \code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}}.}

\item{name1}{Column position of the first character of the person ID
(\verb{NAME1=}). Defaults to \code{1}.}

\item{delimiter_width}{Width of the delimiter separating the person ID
from the first item response, used only to derive the \code{namlen} default.
Defaults to \code{1}; pass \code{nchar(delimiter)} from
\code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}} if you used a longer one.}

\item{namlen}{Width of the person ID field (\verb{NAMLEN=}). Defaults to
\code{item1 - name1 - delimiter_width}, i.e. everything between \code{NAME1} and
\code{ITEM1} except the delimiter itself. Passing \code{id_width} from
\code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}} directly is more direct and always
correct.}

\item{codes}{String of valid single-character response codes
(\verb{CODES=}). Defaults to \code{"01"}.}

\item{iafile}{Optional path for \verb{IAFILE=} (item anchor file).}

\item{idfile}{Optional path for \verb{IDFILE=} (item subset/delete file).}

\item{pfile}{Path for \verb{PFILE=}, the person output file Winsteps will
write. Defaults to \code{"person.out"}.}

\item{ifile}{Optional path for \verb{IFILE=}, the item output file. Worth
requesting even on an anchored run: its displacement column is the
evidence that the anchors were applied rather than re-estimated. See
\code{\link[=winsteps_read_item_output]{winsteps_read_item_output()}}.}

\item{item_labels}{Optional character vector of item labels to list
after \verb{&END}. If supplied, must have length \code{n_items} and be in
data-file column order.}

\item{estimation}{Named list of estimation/convergence control lines,
written as \code{NAME=value;}. Merged over (not replacing) a set of
built-in defaults -- 20 PROX iterations, unlimited JMLE iterations,
converge on logit change, 4 decimal places -- via
\code{\link[utils:modifyList]{utils::modifyList()}}, so passing e.g. \code{list(RCONV = 0.5)} adds
\code{RCONV} alongside the defaults rather than dropping \code{MPROX}/\code{MJMLE}/
etc. Set a built-in key to \code{NULL} to remove it (e.g.
\code{list(MJMLE = NULL)} to omit \verb{MJMLE=} from the control file
entirely). Double check keyword spelling against the Winsteps manual:
an unrecognized keyword is silently ignored by Winsteps rather than
erroring, so e.g. \code{UDECIMALS} (not a real keyword; the correct one is
\code{UDECIM}) has no effect and won't be reported as a mistake.}

\item{tfile}{Optional character vector of Winsteps table numbers to
request via \verb{TFILE=*} ... \verb{*;}.}

\item{extra}{Optional character vector of additional raw control-file
lines to insert before \verb{&END}, for any Winsteps keyword not
otherwise covered here.}
}
\value{
\code{file}, invisibly.
}
\description{
Assembles a Winsteps \code{.ctr} control file from parameters. No exam- or
organization-specific defaults are baked in beyond Winsteps' own
reasonable estimation defaults, which can be overridden via
\code{estimation} and further extended via \code{extra}.
}

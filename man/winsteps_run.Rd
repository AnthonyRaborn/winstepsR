% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{winsteps_run}
\alias{winsteps_run}
\title{Run a Winsteps batch file}
\usage{
winsteps_run(bat_file, wait = TRUE, error_on_failure = TRUE)
}
\arguments{
\item{bat_file}{Path to the \code{.bat} file to run.}

\item{wait}{Whether to block until Winsteps exits. Defaults to \code{TRUE}.}

\item{error_on_failure}{If \code{TRUE} (default), a non-zero exit status from
Winsteps raises an error rather than being returned silently. Only
meaningful when \code{wait = TRUE}, since a status is not available otherwise.}
}
\value{
The integer status code, invisibly.
}
\description{
Runs a \code{.bat} file written by \code{\link[=winsteps_write_bat]{winsteps_write_bat()}}. That file changes to
its own directory on the first line, so this function does not touch the R
session's working directory and several runs can proceed concurrently
without interfering with one another. Winsteps itself only runs on Windows.
}
\details{
A hand-written \code{.bat} without that leading \code{cd} will resolve its control
file and write its output relative to whatever the current directory
happens to be.
}
\examples{
\dontrun{
# Winsteps runs on Windows only, so this cannot be executed here.
options(winstepsR.exe_path = "C:/Winsteps/Winsteps.exe")

bat <- file.path(tempdir(), "run.bat")
winsteps_write_bat(bat, "control.ctr", "OUT.csv")
winsteps_run(bat)
}
}

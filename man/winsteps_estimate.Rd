% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{winsteps_estimate}
\alias{winsteps_estimate}
\title{Estimate person measures with Winsteps, end to end}
\usage{
winsteps_estimate(
  data,
  id_col,
  item_col,
  score_col,
  items = NULL,
  anchor_values = NULL,
  anchors = NULL,
  keep_items = NULL,
  run_id = "winsteps_run",
  working_dir = tempdir(),
  control_args = list(),
  winsteps_exe = getOption("winstepsR.exe_path"),
  run = TRUE
)
}
\arguments{
\item{data}{Long-format response data; see \code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}}.}

\item{id_col, item_col, score_col}{Column names in \code{data}; see
\code{\link[=winsteps_prepare_person_data]{winsteps_prepare_person_data()}}.}

\item{items}{Character vector of all item names, in a fixed order
shared with \code{anchor_values} (and with \code{keep_items}, if used). Supply
this and \code{anchor_values}, or \code{anchors} -- not both.}

\item{anchor_values}{Numeric vector of anchor values, same order as
\code{items}.}

\item{anchors}{A \code{\link[=winsteps_anchors]{winsteps_anchors()}} object, as an alternative to the
\code{items}/\code{anchor_values} pair. Preferred for anything beyond a one-off
call, since it keeps the two vectors from drifting out of order and
validates them once at construction.}

\item{keep_items}{Optional character vector of items to estimate on
(e.g. one domain's items); every other item in \code{items} is excluded
via an \code{IDFILE}. If \code{NULL} (default), all items are used.}

\item{run_id}{A short label for this run, used as a subdirectory name
within \code{working_dir} (e.g. \code{"exam1"}, \code{"exam1_domain2"}). Must contain no
path separators. Give concurrent or repeated runs distinct values --
reusing one overwrites the earlier run's files. Defaults to
\code{"winsteps_run"}.}

\item{working_dir}{Directory under which a \code{run_id} subdirectory is
created for this run's files. Defaults to a temp directory.}

\item{control_args}{Named list of extra arguments passed through to
\code{\link[=winsteps_write_control_file]{winsteps_write_control_file()}} (e.g. \code{estimation}, \code{codes},
\code{extra}). Cannot include arguments this function derives itself
(\code{file}, \code{data_file}, \code{n_items}, \code{item1}, \code{namlen}, \code{iafile}, \code{idfile},
\code{pfile}, \code{ifile}). Use \code{control_args$tfile} (e.g. \code{tfile = "17.1"}) to request
specific Winsteps tables; their output is written to \code{report_file}
and read back into \code{contents$report} (see below).}

\item{winsteps_exe}{Passed through to \code{\link[=winsteps_write_bat]{winsteps_write_bat()}}.}

\item{run}{Whether to actually invoke Winsteps (\code{\link[=winsteps_run]{winsteps_run()}}). Set
to \code{FALSE} to only generate input files (e.g. to inspect them, or to
run Winsteps manually / on a different machine).}
}
\value{
An object of class \code{winsteps_result}, which prints as a summary of
the run rather than dumping every file it read back. Underneath it is a
list carrying \code{run_id} and \code{run_dir}; the paths of every file written
(\code{data_file},
\code{anchor_file}, \code{delete_file}, \code{control_file}, \code{bat_file},
\code{person_file}, \code{item_file}, \code{report_file}); \code{run_at} and \code{elapsed}
recording when the call started and how long it took, plus
\code{winsteps_elapsed} for the Winsteps invocation alone when \code{run = TRUE};
a parallel \code{contents} list holding the
actual lines written to each of those files (\code{contents$data},
\code{contents$anchor}, \code{contents$delete}, \code{contents$control},
\code{contents$bat}, and, if \code{run = TRUE}, \code{contents$person} -- the raw
PFILE lines Winsteps wrote -- and \code{contents$report} -- the raw lines
of any \verb{TFILE=}-requested tables, e.g. Table 17.1, written to
\code{report_file}); and, if \code{run = TRUE}, \code{results}: the tibble from
\code{\link[=winsteps_read_person_output]{winsteps_read_person_output()}}. \code{working_dir} defaults to a temp
directory that the OS is free to clear, so \code{contents} lets callers
inspect exactly what was written/read without depending on those
paths still being valid later.
}
\description{
A convenience wrapper around the individual \verb{winsteps_write_*()} /
\code{\link[=winsteps_run]{winsteps_run()}} / \code{\link[=winsteps_read_person_output]{winsteps_read_person_output()}} functions for the
common case: given long-format response data and a fixed set of
anchored item difficulties, estimate person ability (optionally on a
subset of items, e.g. one content domain) and return the result.
}
\details{
Files for a call are written to the \code{run_id} subdirectory of
\code{working_dir}, so calls with distinct \code{run_id}s (e.g. one per exam, one per
domain) don't overwrite each other's intermediate files. Paths are
deterministic rather than unique: two calls sharing a \code{run_id} and
\code{working_dir} write to the same directory, and the second overwrites the
first.

When \code{run = TRUE}, a failed Winsteps run raises an error rather
than returning an empty result: a non-zero exit status errors via
\code{\link[=winsteps_run]{winsteps_run()}}, and a run that reports success but writes no PFILE
errors here. A PFILE containing only its header is \emph{not} a failure --
that is Winsteps' normal output for a cohort with no estimable persons,
and it yields a zero-row \code{results} tibble.
}
\examples{
responses <- data.frame(
  person_id = rep(c("00001", "00002"), each = 2),
  item      = rep(c("q1", "q2"), 2),
  score     = c(1, 0, 1, 1)
)

# Generate the Winsteps input files without running Winsteps, which is
# possible on any platform.
result <- winsteps_estimate(
  data = responses,
  id_col = "person_id", item_col = "item", score_col = "score",
  anchors = winsteps_anchors(c("q1", "q2"), c(-0.4, 0.6)),
  run_id = "example_run", winsteps_exe = "Winsteps.exe", run = FALSE
)
result$contents$control

}

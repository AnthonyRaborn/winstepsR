% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_control.R
\name{winsteps_read_control_file}
\alias{winsteps_read_control_file}
\title{Read a Winsteps control file into the arguments that wrote it}
\usage{
winsteps_read_control_file(file)
}
\arguments{
\item{file}{Path to the control file.}
}
\value{
An object of class \code{winsteps_control}: a named list of arguments for
\code{\link[=winsteps_write_control_file]{winsteps_write_control_file()}}, usable directly with \code{\link[=do.call]{do.call()}}.
}
\description{
Parses an existing \code{.ctr} file into the arguments
\code{\link[=winsteps_write_control_file]{winsteps_write_control_file()}} takes, so an established Winsteps setup can
be brought into R rather than rebuilt by hand:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{args <- winsteps_read_control_file("existing.ctr")
args$estimation$LCONV <- 0.001
do.call(winsteps_write_control_file, c(list(file = "new.ctr"), args))
}\if{html}{\out{</div>}}

Values keep their case and their leading zeros. Both matter: lowercasing a
value corrupts file paths on a case-sensitive filesystem and any \verb{TITLE=}
text, and coercing \code{CODES=01} to a number turns two valid response codes
into one.
}
\section{What is recognized}{


Keywords with a dedicated argument (\code{DATA}, \code{NI}, \code{ITEM1}, \code{NAME1},
\code{NAMLEN}, \code{CODES}, \code{IAFILE}, \code{IDFILE}, \code{PFILE}, \code{IFILE}, \code{TFILE}) are
returned under that argument's name. Item labels between \verb{&END} and
\verb{END LABELS} become \code{item_labels}. Every other \code{KEYWORD=value} line becomes
an entry in \code{estimation}, which is where \code{\link[=winsteps_write_control_file]{winsteps_write_control_file()}}
will write it back. Comments -- everything after a \verb{;} -- are discarded,
along with anything after \verb{END LABELS}.

\verb{ITEM=} is dropped, because \code{\link[=winsteps_write_control_file]{winsteps_write_control_file()}} always writes
\code{ITEM=Item;} itself and returning it as well would emit the keyword twice.
A value other than \code{Item} warns rather than disappearing silently.
}

\examples{
ctr <- system.file("extdata", "example_full_control.ctr", package = "winstepsR")
args <- winsteps_read_control_file(ctr)
args
args$n_items
args$estimation$LCONV
}

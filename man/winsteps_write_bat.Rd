% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{winsteps_write_bat}
\alias{winsteps_write_bat}
\title{Write a Winsteps batch (.bat) runner file}
\usage{
winsteps_write_bat(
  file,
  control_file,
  out_file,
  winsteps_exe = getOption("winstepsR.exe_path"),
  extra_args = "HLINES=YES"
)
}
\arguments{
\item{file}{Path to write the \code{.bat} file to.}

\item{control_file}{Control file name/path, as Winsteps will see it
(typically relative to \code{file}'s directory, since Winsteps is run from
that directory).}

\item{out_file}{Name of the batch report file Winsteps writes on exit
(its \code{BATCH=YES} third argument), e.g. \code{"OUT.csv"}.}

\item{winsteps_exe}{Path to \code{Winsteps.exe}. Defaults to
\code{getOption("winstepsR.exe_path")}, so a project can set this once via
\code{options(winstepsR.exe_path = "C:/Winsteps/Winsteps.exe")} instead of
hardcoding it at every call site.}

\item{extra_args}{Additional batch arguments appended verbatim, e.g.
\code{"HLINES=YES"}.}
}
\value{
\code{file}, invisibly.
}
\description{
Write a Winsteps batch (.bat) runner file
}
\details{
Paths are quoted for \code{cmd.exe} regardless of the platform this
function runs on, so a \code{.bat} generated on macOS or Linux and handed to a
Windows machine (see \code{run = FALSE} in \code{\link[=winsteps_estimate]{winsteps_estimate()}}) is valid
there.

The batch file begins by changing to its own directory, since Winsteps
writes its output relative to wherever it was launched. That makes the
file self-locating: it can be run from any working directory, by
\code{\link[=winsteps_run]{winsteps_run()}} or by double-clicking it, and still finds its control
file and leaves its output alongside itself.
}
\examples{
f <- tempfile(fileext = ".bat")
winsteps_write_bat(
  f, control_file = "control.ctr", out_file = "OUT.csv",
  winsteps_exe = "C:/Winsteps/Winsteps.exe"
)

# the leading cd makes the file self-locating, and paths are quoted for
# cmd.exe whatever platform generated them
writeLines(readLines(f))

unlink(f)
}

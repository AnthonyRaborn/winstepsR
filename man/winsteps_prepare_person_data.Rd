% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_file.R
\name{winsteps_prepare_person_data}
\alias{winsteps_prepare_person_data}
\title{Pivot long-format item response data into Winsteps person-data rows}
\usage{
winsteps_prepare_person_data(
  data,
  id_col,
  item_col,
  score_col,
  id_width = NULL,
  delimiter = "*",
  missing_code = ".",
  item_order = NULL
)
}
\arguments{
\item{data}{A data frame in long format with one row per person-item
response.}

\item{id_col}{Name of the person identifier column in \code{data}.}

\item{item_col}{Name of the item identifier column in \code{data}.}

\item{score_col}{Name of the (numeric-coercible) score column in \code{data}.}

\item{id_width}{Fixed width to pad/left-justify the person ID to. If
\code{NULL} (default), the width of the longest ID actually present in the
filtered data is used.}

\item{delimiter}{Single-character (or short string) separator written
between the ID field and the first item response. Defaults to \code{"*"}.}

\item{missing_code}{Code written for a person-item combination with no
response. Defaults to \code{"."}.}

\item{item_order}{Optional character vector giving the exact, ordered
set of items to include (and their column order). If omitted, items
are ordered as they naturally sort after pivoting. Supplying this
explicitly is recommended so that the anchor file and data file stay
in sync.}
}
\value{
A list with:
\describe{
\item{lines}{Character vector, one fixed-format line per person.}
\item{items}{Character vector of item names, in file column order.}
\item{n_items}{Number of items (\code{NI} in Winsteps terms).}
\item{id_width}{Width used for the ID field (\code{NAMLEN}).}
\item{item1}{Column position of the first item response (\code{ITEM1}).}
\item{delimiter}{The delimiter used.}
}
}
\description{
Winsteps expects one fixed-format line per person: a left-justified,
fixed-width person ID, followed by a delimiter, followed by one
character (or fixed-width code) per item in a known, stable order.
This function performs that reshape without assuming anything about
item names, ID format, or exam identity.
}
